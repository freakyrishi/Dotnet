/*

using System;
using System.Collections;


namespace ConsoleApp1
{
    internal class Class1
    {
        static void Main()
        {
            Stack st=new Stack();
            st.Push("Shyam");
            st.Push("rohit");
            st.Push("mahul");
            st.Push("vicky");
            st.Push("shubham");
            st.Push("ankit");

            foreach (string i in st)
            {
                Console.WriteLine(i);
            }

            st.Push("1");
            st.Push("2");

            Console.WriteLine("latest value in last postion = " +st.Peek());
            Console.WriteLine();

            foreach(string g in st)
            {
                Console.WriteLine(g);
            }
            Console.WriteLine("**********************************************");

            st.Pop();
            st.Pop();
            st.Pop();
            st.Pop();
            st.Pop();
            st.Pop();

            foreach(string g1 in st)
            {
                Console.WriteLine(g1);
            }

        }
    }
}*/
